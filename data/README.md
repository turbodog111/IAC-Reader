# Local data

`attempts.jsonl` and `flags.jsonl` are created by the local server and excluded from version control. The app also maintains a browser retry cache. Use **Backup** before moving to another machine or resetting browser data; **Import** restores both files without duplicating existing record IDs.
